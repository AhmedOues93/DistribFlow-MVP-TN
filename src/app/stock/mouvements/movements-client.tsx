"use client";import { useEffect,useState } from "react";
const movementLabels:Record<string,string>={PURCHASE_RECEIPT:"Réception",SALE:"Sortie commande",RETURN:"Retour",DAMAGE:"Avarie",ADJUSTMENT:"Correction",TRANSFER_IN:"Transfert entrant",TRANSFER_OUT:"Transfert sortant"};
export default function MovementsClient(){const [data,setData]=useState<any>({items:[]}),[query,setQuery]=useState(""),[error,setError]=useState("");useEffect(()=>{fetch(`/api/inventory/movements?q=${encodeURIComponent(query)}`).then(async r=>{const d=await r.json();if(!r.ok)setError(d.error);else setData(d)}).catch(()=>setError("Impossible de charger les mouvements"))},[query]);return <main className="content"><div className="page-heading"><div><p className="eyebrow">Stock</p><h1>Mouvements</h1><p>Historique immuable des entrées, sorties et transferts.</p></div></div>{error&&<div className="form-error">{error}</div>}<section className="panel customer-panel"><div className="customer-toolbar"><label className="search-field">⌕<input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Référence, note ou produit"/></label></div><div className="table-scroll"><table><thead><tr><th>Date</th><th>Produit</th><th>Entrepôt</th><th>Type</th><th>Quantité</th><th>Référence</th><th>Notes</th></tr></thead><tbody>{data.items.map((item:any)=><tr key={item.id}><td>{new Date(item.createdAt).toLocaleString("fr-TN")}</td><td>{item.product.name}</td><td>{item.warehouse.name}</td><td>{movementLabels[item.type]??item.type}</td><td>{String(item.quantity)}</td><td>{item.reference||"—"}</td><td>{item.notes||"—"}</td></tr>)}</tbody></table></div>{!data.items.length&&<div className="empty-state"><strong>Aucun mouvement</strong><p>Les opérations de stock apparaîtront ici.</p></div>}</section></main>}
