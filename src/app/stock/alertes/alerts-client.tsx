"use client";import { useEffect,useState } from "react";
export default function AlertsClient(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{fetch("/api/inventory/low-stock").then(r=>r.json()).then(setItems)},[]);return <main className="content"><div className="page-heading"><div><p className="eyebrow">Stock</p><h1>Alertes de stock faible</h1><p>Produits dont le niveau atteint leur seuil minimum.</p></div></div><section className="panel customer-panel"><div className="table-scroll"><table><thead><tr><th>Produit</th><th>Entrepôt</th><th>Disponible</th><th>Seuil</th></tr></thead><tbody>{items.map(item=><tr key={item.id}><td><strong>{item.product.name}</strong></td><td>{item.warehouse.name}</td><td>{String(item.quantity)}</td><td>{String(item.product.minimumStock)}</td></tr>)}</tbody></table></div>{!items.length&&<div className="empty-state"><strong>Aucune alerte</strong><p>Les seuils de stock sont respectés.</p></div>}</section></main>}
