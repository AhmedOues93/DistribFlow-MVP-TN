import { NextRequest,NextResponse } from "next/server";import { lowStock } from "@/lib/services/inventory";import { requireRequestContext } from "@/lib/services/auth";export async function GET(r:NextRequest){try{return NextResponse.json(await lowStock(await requireRequestContext(r)))}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Erreur serveur"},{status:400})}}
