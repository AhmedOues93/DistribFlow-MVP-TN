import { NextRequest,NextResponse } from "next/server";import { listMovements } from "@/lib/services/inventory";import { requireRequestContext } from "@/lib/services/auth";export async function GET(r:NextRequest){try{const p=r.nextUrl.searchParams;return NextResponse.json(await listMovements(await requireRequestContext(r),{productId:p.get("productId")??undefined,warehouseId:p.get("warehouseId")??undefined,query:p.get("q")??undefined,page:Number(p.get("page")??1)}))}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Erreur serveur"},{status:400})}}
