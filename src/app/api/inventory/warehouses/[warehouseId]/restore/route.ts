import { NextRequest,NextResponse } from "next/server";import { setWarehouseActive } from "@/lib/services/inventory";import { requireRequestContext } from "@/lib/services/auth";export async function POST(r:NextRequest,{params}:{params:Promise<{warehouseId:string}>}){try{return NextResponse.json(await setWarehouseActive((await params).warehouseId,true,await requireRequestContext(r)))}catch(e){return NextResponse.json({error:e instanceof Error?e.message:"Erreur serveur"},{status:400})}}
