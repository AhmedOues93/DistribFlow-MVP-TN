"use client";
import { useEffect,useState } from "react";
export default function StockClient(){const [levels,setLevels]=useState<any[]>([]),[error,setError]=useState("");useEffect(()=>{fetch("/api/inventory/stock").then(async r=>{const d=await r.json();if(!r.ok)setError(d.error);else setLevels(d)}).catch(()=>setError("Impossible de charger le stock"))},[]);return <main className="content"><div className="page-heading"><div><p className="eyebrow">Inventaire</p><h1>Stock actuel</h1><p>Quantités disponibles par produit et entrepôt.</p></div></div>{error&&<div className="form-error">{error}</div>}<section className="panel customer-panel"><div className="table-scroll"><table><thead><tr><th>Produit</th><th>Entrepôt</th><th>Quantité</th><th>Seuil minimum</th><th>État</th></tr></thead><tbody>{levels.map(level=><tr key={level.id}><td><strong>{level.product.name}</strong></td><td>{level.warehouse.name}</td><td>{String(level.quantity)} {level.product.unit}</td><td>{String(level.product.minimumStock)}</td><td>{Number(level.quantity)<=Number(level.product.minimumStock)?"Stock faible":"Disponible"}</td></tr>)}</tbody></table></div>{!levels.length&&<div className="empty-state"><strong>Aucun mouvement de stock</strong><p>Les quantités apparaîtront après une entrée ou un transfert.</p></div>}</section></main>}
